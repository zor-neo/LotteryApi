{
  "name": "lottery-api-cloudflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:schema": "wrangler secret put DATABASE_URL && echo Apply migrations/current-neon-schema.sql to Neon with your SQL client"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "^5.7.2",
    "vitest": "^4.1.9",
    "wrangler": "^4.25.0"
  }
}
